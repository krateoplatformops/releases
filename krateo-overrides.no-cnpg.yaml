# Krateo release profile.
# This file defines the no-cnpg override profile for Krateo releases.
# Use with krateoctl plan/apply.
# It disables the CNPG component so Krateo can use an existing PostgreSQL database.

components:
  cnpg:
    enabled: false
